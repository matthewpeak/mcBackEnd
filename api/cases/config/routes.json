{
  "routes": [
    {
      "method": "GET",
      "path": "/cases",
      "handler": "cases.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cases",
      "handler": "cases.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cases",
      "handler": "cases.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
